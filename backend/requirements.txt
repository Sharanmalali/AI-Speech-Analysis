# ---------------------------------------------------------------------------
# AblePro Solutions — Backend dependencies (Python 3.12)
# Pinned for reproducible, production-grade builds.
# ---------------------------------------------------------------------------

# --- Web framework / ASGI ---------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20

# --- Data validation / settings --------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database / ORM / migrations -------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3          # PostgreSQL (Supabase) driver

# --- Auth / security --------------------------------------------------------
PyJWT==2.10.1
bcrypt==4.2.1
cryptography==44.0.0

# --- Supabase (Auth / Storage) ---------------------------------------------
supabase==2.11.0
#storage3==0.9.0

# --- Caching / queue / background tasks ------------------------------------
redis==5.2.1
celery==5.4.0
flower==2.0.1

# --- Rate limiting ----------------------------------------------------------
slowapi==0.1.10

# --- HTTP client ------------------------------------------------------------
httpx==0.28.1

# --- Machine learning runtime ----------------------------------------------
scikit-learn==1.9.0             # loads gender_age_clf (1.9.0) + atypicality (1.6.1)
joblib==1.5.3
numpy==2.2.1
scipy==1.15.0
pandas==2.2.3                   # named-feature DataFrames for sklearn inference
transformers==4.46.3            # wav2vec2 audio-classification (gender + age models)

# --- Audio / speech ---------------------------------------------------------
torch==2.5.1
torchaudio==2.5.1
pyannote.audio==3.3.2
#openai-whisper==20240930
faster-whisper==1.1.0
librosa==0.10.2.post1
soundfile==0.12.1
praat-parselmouth==0.4.6
noisereduce==3.0.3
ffmpeg-python==0.2.0
pydub==0.25.1

# --- Reporting / PDF --------------------------------------------------------
reportlab==4.2.5
matplotlib==3.10.0

# --- LLM (optional gender/age fallback) ------------------------------------
google-genai>=1.0.0,<2.0.0

# --- Observability / utils --------------------------------------------------
structlog==24.4.0
tenacity==9.0.0
python-dotenv==1.0.1

huggingface_hub==0.23.4
