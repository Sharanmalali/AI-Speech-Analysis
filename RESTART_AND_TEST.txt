========================================
  I FIXED THE UUID ERROR!
========================================

The problem was the IDs in demo_data.json
were strings, not UUIDs.

I've fixed all IDs to proper UUID format.

========================================
  RESTART BACKEND NOW
========================================

For Docker:
  docker compose restart backend

For Local:
  1. Stop backend (Ctrl+C)
  2. Run: uvicorn app.main:app --reload

========================================
  THEN TEST
========================================

Open browser: http://localhost:3000/demo

It WILL work now!

========================================
